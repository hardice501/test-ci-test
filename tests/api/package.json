{
  "name": "@ci-test/api-tests",
  "private": true,
  "version": "2.0.1",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"process.exit(0)\""
  }
}
