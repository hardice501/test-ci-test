{
  "name": "@ci-test/common",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"process.exit(0)\""
  }
}
